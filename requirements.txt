numpy>=1.26
matplotlib>=3.8
PyYAML>=6.0

# ROS 2 Jazzy Python packages become visible after setup.bash is sourced.
# Install their runtime/build helpers inside the isolated course venv too.
setuptools>=68.1
Jinja2>=3.1
typeguard>=4.1
